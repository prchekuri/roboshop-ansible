- name: Load MySQL Schema
  community.mysql.mysql_db:
    state: import
    name: all
    target: /app/schema/{{component}}.sql
    login_host: mysql.roboshop.internal
    login_user: root
    login_password: "{{ root_mysql_password }}"